# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: Apple Korea (MacBook Pro 14 & iPad Pro)
- **분석 날짜**: 2025-10-18
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 프리미엄 미니멀리즘, 다크 모드 중심

---

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary: #0071E3;
  --color-primary-light: #0077ED;
  --color-primary-dark: #006EDB;

  /* Secondary Colors */
  --color-secondary: #000000;
  --color-accent: #147CE5;

  /* Background Colors */
  --color-bg-primary: #000000;
  --color-bg-secondary: #1D1D1F;
  --color-bg-tertiary: #2C2C2E;
  --color-bg-card: #1C1C1E;

  /* Text Colors */
  --color-text-primary: #FFFFFF;
  --color-text-secondary: #F5F5F7;
  --color-text-tertiary: #A1A1A6;
  --color-text-muted: #86868B;

  /* Border Colors */
  --color-border-primary: #424245;
  --color-border-secondary: #3A3A3C;
  --color-border-hover: #515154;

  /* Interactive Colors */
  --color-button-primary: #0071E3;
  --color-button-hover: #0077ED;
  --color-button-active: #006EDB;
  --color-link: #147CE5;
  --color-link-hover: #0077ED;
}
```

---

## 🎨 컬러 사용 가이드

### **Primary Blue (#0071E3)**
**사용 위치:**
- 주요 CTA(Call-to-Action) 버튼
- 중요한 링크 및 액션
- 브랜드 포인트 요소

**사용 예시:**
```css
.btn-primary {
  background-color: var(--color-button-primary);
  color: var(--color-text-primary);
}

.btn-primary:hover {
  background-color: var(--color-button-hover);
}

.btn-primary:active {
  background-color: var(--color-button-active);
}
```

**주의사항:**
- 페이지당 2-3개의 핵심 액션에만 사용
- 과도한 사용 시 시각적 임팩트 감소

---

### **Black (#000000) - 배경**
**사용 위치:**
- 메인 배경색
- 히어로 섹션
- 풀스크린 배경

**사용 예시:**
```css
body {
  background-color: var(--color-bg-primary);
  color: var(--color-text-primary);
}

.hero-section {
  background-color: var(--color-bg-primary);
}
```

**디자인 효과:**
- 프리미엄한 느낌 연출
- 콘텐츠에 집중도 향상
- 제품/이미지를 돋보이게 함

---

### **White (#FFFFFF) - 텍스트**
**사용 위치:**
- 주요 헤딩 (H1, H2)
- 중요한 정보
- 강조 텍스트

**사용 예시:**
```css
h1, h2 {
  color: var(--color-text-primary);
  font-weight: 600;
}

.highlight-text {
  color: var(--color-text-primary);
}
```

---

### **Gray Tones - 계층 구조**
**사용 위치:**
- `#F5F5F7`: 본문 텍스트
- `#A1A1A6`: 서브 텍스트, 캡션
- `#86868B`: 보조 정보, 메타데이터

**사용 예시:**
```css
p {
  color: var(--color-text-secondary);
}

.caption, .subtitle {
  color: var(--color-text-tertiary);
}

.metadata, .timestamp {
  color: var(--color-text-muted);
}
```

---

### **Secondary Button (Ghost Button)**
**사용 위치:**
- 부차적인 액션 (가격 보기, 자세히 알아보기)
- 취소 버튼
- 옵션 선택

**사용 예시:**
```css
.btn-secondary {
  background-color: transparent;
  border: 1px solid var(--color-border-primary);
  color: var(--color-text-primary);
}

.btn-secondary:hover {
  border-color: var(--color-border-hover);
  background-color: rgba(255, 255, 255, 0.05);
}
```

---

## 📱 반응형 고려사항

### **다크모드 / 라이트모드 전환**

```css
/* 다크모드 (기본) */
:root {
  --color-bg-primary: #000000;
  --color-text-primary: #FFFFFF;
}

/* 라이트모드 */
:root[data-theme="light"] {
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F5F5F7;
  --color-bg-tertiary: #E8E8ED;

  --color-text-primary: #1D1D1F;
  --color-text-secondary: #424245;
  --color-text-tertiary: #6E6E73;
  --color-text-muted: #86868B;

  --color-border-primary: #D2D2D7;
  --color-border-secondary: #E8E8ED;

  /* Primary Blue는 동일하게 유지 */
  --color-button-primary: #0071E3;
}
```

### **모바일 환경 최적화**

```css
/* 터치 디바이스에서 버튼 크기 증가 */
@media (max-width: 768px) {
  .btn-primary,
  .btn-secondary {
    min-height: 44px; /* 터치 타겟 최소 크기 */
    padding: 12px 24px;
    font-size: 16px;
  }
}
```

### **고대비 모드 지원**

```css
/* 접근성: 고대비 모드 */
@media (prefers-contrast: high) {
  :root {
    --color-text-primary: #FFFFFF;
    --color-text-secondary: #FFFFFF;
    --color-bg-primary: #000000;
    --color-border-primary: #FFFFFF;
  }

  .btn-primary {
    border: 2px solid var(--color-text-primary);
  }
}
```

### **프리퍼 컬러 스킴 감지**

```css
/* 시스템 다크모드 자동 감지 */
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-primary: #000000;
    --color-text-primary: #FFFFFF;
  }
}

@media (prefers-color-scheme: light) {
  :root {
    --color-bg-primary: #FFFFFF;
    --color-text-primary: #1D1D1F;
  }
}
```

---

## 🎯 실전 컴포넌트 예시

### **1. 히어로 섹션**
```css
.hero {
  background-color: var(--color-bg-primary);
  padding: 80px 20px;
  text-align: center;
}

.hero h1 {
  color: var(--color-text-primary);
  font-size: 56px;
  font-weight: 600;
  margin-bottom: 16px;
}

.hero p {
  color: var(--color-text-tertiary);
  font-size: 21px;
  margin-bottom: 32px;
}
```

### **2. 카드 컴포넌트**
```css
.card {
  background-color: var(--color-bg-secondary);
  border: 1px solid var(--color-border-secondary);
  border-radius: 12px;
  padding: 24px;
  transition: all 0.3s ease;
}

.card:hover {
  border-color: var(--color-border-hover);
  transform: translateY(-4px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
}

.card-title {
  color: var(--color-text-primary);
  font-size: 24px;
  margin-bottom: 12px;
}

.card-description {
  color: var(--color-text-tertiary);
  font-size: 16px;
  line-height: 1.6;
}
```

### **3. 네비게이션 바**
```css
.navbar {
  background-color: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(20px);
  border-bottom: 1px solid var(--color-border-secondary);
  padding: 12px 20px;
}

.nav-link {
  color: var(--color-text-secondary);
  text-decoration: none;
  font-size: 14px;
  transition: color 0.2s ease;
}

.nav-link:hover {
  color: var(--color-text-primary);
}

.nav-link.active {
  color: var(--color-text-primary);
}
```

### **4. 버튼 그룹**
```css
.button-group {
  display: flex;
  gap: 16px;
  margin-top: 24px;
}

.btn {
  padding: 12px 24px;
  border-radius: 980px;
  font-size: 17px;
  font-weight: 400;
  cursor: pointer;
  transition: all 0.2s ease;
  text-decoration: none;
  display: inline-block;
}

.btn-primary {
  background-color: var(--color-button-primary);
  color: var(--color-text-primary);
  border: none;
}

.btn-primary:hover {
  background-color: var(--color-button-hover);
}

.btn-secondary {
  background-color: transparent;
  color: var(--color-primary);
  border: 1px solid var(--color-border-primary);
}

.btn-secondary:hover {
  border-color: var(--color-border-hover);
}
```

---

## 💡 접근성 가이드라인

### **컬러 대비 비율**
- **WCAG AA 기준 준수**
  - 일반 텍스트: 최소 4.5:1
  - 큰 텍스트 (18px+): 최소 3:1
  - UI 요소: 최소 3:1

### **테스트된 대비 비율**
- `#FFFFFF` on `#000000`: 21:1 ✅
- `#0071E3` on `#000000`: 4.85:1 ✅
- `#A1A1A6` on `#000000`: 4.52:1 ✅

### **포커스 표시**
```css
/* 키보드 포커스 명확하게 표시 */
button:focus-visible,
a:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}
```

---

## 🚀 적용 체크리스트

- [ ] CSS 변수를 프로젝트 메인 스타일시트에 추가
- [ ] 다크모드/라이트모드 토글 기능 구현
- [ ] 모든 버튼에 호버/액티브 스타일 적용
- [ ] 접근성 대비 비율 테스트 완료
- [ ] 모바일 반응형 확인
- [ ] 크로스 브라우저 테스트
- [ ] 고대비 모드 테스트

---

## 📚 참고 자료
- [Apple Design Resources](https://developer.apple.com/design/resources/)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [CSS Custom Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/--*)

---

**Last Updated**: 2025-10-18
**Version**: 1.0.0
